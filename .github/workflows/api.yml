name: 'API staging'
on:
  push:
    branches:
      - staging
    paths:
      - packages/api/**

jobs:
  build:
    environment: staging
    uses: k9rosie/peddl/.github/workflows/build.yml@main
    with:
      project: api
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
